import React from "react";
import Input from "./Input";

function DepartureComponent({ onClick }) {
  return <Input placeholder="Departure" onClick={onClick} />;
}

export default DepartureComponent;
